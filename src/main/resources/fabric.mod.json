{
  "schemaVersion": 1,
  "id": "charmony-decor",
  "version": "${mod_version}",

  "name": "Charmony Decor",
  "description": "Decoration (tbc) and sitting on stair blocks.",

  "authors": [
    "svenhjol"
  ],
  "icon": "assets/charmony/textures/gui/sprites/charmony-decor.png",
  "license": "MIT",
  "environment": "*",
  "accessWidener": "charmony-decor.accesswidener",
  "entrypoints": {
    "main": [
      "svenhjol.charmony.decor.common.CommonInitializer"
    ],
    "client": [
      "svenhjol.charmony.decor.client.ClientInitializer"
    ]
  },

  "mixins": [],

  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "*",
    "java": ">=${java_version}",
    "fabric-api": "*",
    "charmony": ">=${charmony_version}",
    "charmony-api": ">=${api_version}"
  },

  "contact": {
    "homepage": "${homepage_link}",
    "sources": "https://github.com/svenhjol/${mod_id}"
  },

  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_link}"
      },
      "update_checker": true
    }
  }
}